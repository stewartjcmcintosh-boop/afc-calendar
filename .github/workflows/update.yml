name: Update AFC Calendar

on:
  schedule:
    - cron: '0 */6 * * *'

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Update ICS
      run: |
        echo "Rebuilding ICS..."
        # Here you'd plug in script (I can build this next)

    - name: Commit changes
      run: |
        git config user.name "bot"
        git config user.email "bot@github.com"
        git add .
        git commit -m "Update fixtures" || exit 0
        git push
